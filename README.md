# CORS Misconfiguration Scanner

An offensive security reconnaissance tool that detects common CORS misconfigurations by analyzing HTTP response headers.

## Features
- Detects wildcard and reflected origins
- Flags insecure credential configurations
- Generates a basic findings report

## Usage
```bash
pip3 install requests
python3 scanner.py

